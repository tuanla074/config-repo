spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/microlab
    username: postgres
    password: 074201
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
